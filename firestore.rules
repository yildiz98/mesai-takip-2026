rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /smartapart/{document=**} {
      allow read, write: if request.auth != null;
    }
    match /mesaiTakip/{document=**} {
      allow read, write: if request.auth != null;
    }
    match /users/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
